apiVersion: apps/v1
kind: Deployment
metadata:
  name: kitchen-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kitchen-service
  template:
    metadata:
      labels:
        app: kitchen-service
    spec:
      containers:
        - name: kitchen-service
          image: go-coffeeshop-kitchen
          imagePullPolicy: Never
          env:
            - name: APP_NAME
              value: "kitchen-service in k8s"
            - name: IN_DOCKER
              value: "true"
            - name: PG_URL
              valueFrom:
                secretKeyRef:
                  name: coffeeshop-secrets
                  key: PG_URL
            - name: PG_DSN_URL
              valueFrom:
                secretKeyRef:
                  name: coffeeshop-secrets
                  key: PG_DSN_URL
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: coffeeshop-secrets
                  key: RABBITMQ_URL
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: kitchen-service
spec:
  type: NodePort       
  selector:
    app: kitchen-service
  ports:
    - port: 80
      targetPort: 80    
      nodePort: 30080
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: kitchen-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: kitchen-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70